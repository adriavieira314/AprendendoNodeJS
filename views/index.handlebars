<div class="jumbotron">
  <h1 class="display-4">Bem-Vindo ao Blog do Node!</h1>
  <p class="lead">Este é um Blog simples, feito com NodeJS + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui você vai poder ler sobre as novidades do mundo da computação.</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes</h2>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h3>{{ titulo }}</h3>
            <small>{{ descricao }}</small><br>
            <a href="/home/postagens/{{slug}}">
                <button class="btn btn-success">Leia Mais</button>
            </a>
            <hr>
            <small><strong>Categoria: </strong>{{ categoria.nome }}</small><br>
            <small><strong>Data de Publicação: </strong>{{ date }}</small>
        </div>
    </div>
{{else}}
    <h4 class="mt-3">Nenhuma postagem registrada!</h4>
{{/each}}